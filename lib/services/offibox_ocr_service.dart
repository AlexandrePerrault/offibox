/// Service OCR (Tesseract) pour extraire codes 7 depuis une image PNG.
class OffiboxOcrService {
  /// Extrait les codes 7 (ex. CIP) depuis les bytes PNG de l'image.
  static Future<List<String>> extractCode7FromPngBytes(List<int> pngBytes) async {
    return [];
  }
}
