/// Service de chargement des prix catalogue (CSV CERP équipement).
class CataloguePriceCatalogService {
  /// Charge le mapping (réf → prix) depuis une URL CSV.
  static Future<Map<String, double>> loadFromCsvUrl(String url) async {
    return {};
  }
}
