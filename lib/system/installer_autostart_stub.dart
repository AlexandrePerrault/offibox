// Stub for non-Windows or when FLUTTER_BUILD_WINDOWS is false.
// Windows build uses installer_autostart_windows.dart.

void applyFromInstaller() {
  // No-op: autostart is only applied on Windows build via installer.
}
